<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sjtu.dolo.mapper.StudentMapper">
    <select id="getAllSection" resultType="sjtu.dolo.model.Section">
        select * from course.section;
    </select>
    <insert id="addTakes" parameterType="sjtu.dolo.model.Takes">
        insert into course.takes (secID, semester, year, timeslotID, courseID, user_name, grade, gpa) VALUES (#{secID},#{semester},#{year},#{timeSlotID},#{courseID},#{user_name},#{grade},#{gpa});
    </insert>
    <delete id="delTakes" parameterType="sjtu.dolo.model.Takes">
        delete from course.takes where secID=#{secID} and semester=#{semester} and year=#{year} and timeslotID=#{timeSlotID} and courseID=#{courseID} and user_name=#{user_name};
    </delete>
    <select id="getAllTakes" resultType="sjtu.dolo.model.Takes">
        select * from course.takes;
    </select>
</mapper>
